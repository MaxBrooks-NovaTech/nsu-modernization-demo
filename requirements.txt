# Phase 2 dbt transformation layer.
# Keep this file so local setup can consistently run:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt

dbt-core==1.10.13
dbt-postgres==1.9.0
